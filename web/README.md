# 网页版使用说明

这是 `StudyPlanAnalyzer` 的本地单页网页版本，不需要安装前端依赖。

## 启动

在 `web` 目录中双击 `run.bat`，然后打开 <http://127.0.0.1:8765>。

也可以直接双击 `index.html` 打开。使用本地服务器时，浏览器的密码加密和本地存储行为更稳定。

## 数据说明

网页版本使用浏览器 `localStorage` 保存账号、任务和执行记录，所有记录都带有账号标识并在页面查询时隔离。清理浏览器站点数据会删除本机保存的网页数据；可以在“我的”页面先导出 CSV 备份。
